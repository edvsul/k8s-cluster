apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "l4-rule"
  namespace: "linkding"
spec:
  endpointSelector:
    matchLabels:
      app: linkding
  egress:
    - toPorts:
      - ports:
        - port: "80"
          protocol: TCP

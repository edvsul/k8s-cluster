apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "l4-rule"
  namespace: "linkding"
spec:
  endpointSelector:
    matchLabels:
      app: linkding
  egress:
    - toEntities:
      - world
      egressDeny:
      - toEntities:
        - world

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reflector
  namespace: reflector
spec:
  interval: 30m
  chart:
    spec:
      chart: reflector
      version: "10.0.55"
      sourceRef:
        kind: HelmRepository
        name: emberstack
        namespace: reflector
      interval: 12h
  values:
    securityContext:
      allowPrivilegeEscalation: false
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        memory: 256Mi
